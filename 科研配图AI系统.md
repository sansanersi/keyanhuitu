## 1）线稿图着色（Line Art Colorization）

**工作内容**：黑白科研线稿（细胞、分子、器件、流程）自动 / 辅助上色，色彩规范、边界准确、层次自然。

### 相关算法 / 技术

- Sketch Colorization（线稿上色）
  - **CNN/UNet**：提取轮廓、区域分割、逐像素上色（如 PaintsChainer）
  - **Diffusion 模型（Stable Diffusion/SDXL）**：“线稿 + 提示词” 生成彩色图；控制强度 / 区域更灵活
  - **SketchDeco（CVPR2026）**：**无训练、区域掩码 + 调色板**，latent blending + self-attention，保结构、全局和谐
- 色彩规范算法
  - **色盲友好配色**（Colorbrewer）：Deuteranopia/Protanopia 安全色，科研期刊强推荐
  - **语义配色规则库**：C = 灰 / 黑、H = 白、O = 红、N = 蓝、S = 黄、P = 橙；细胞 / 细胞器固定色卡

### 常用工具 / 模型

- PaintsChainer、SketchDeco、SDXL（ControlNet 线稿控制）
- Adobe Illustrator + 色彩脚本、Inkscape

### 实现思路

1. 线稿 → 边缘检测 / 二值化 → 区域掩码（SAM/OpenCV）
2. 按学科语义分配基础色（分子 / 细胞 / 材料）
3. Diffusion/CNN 填充渐变与光影
4. 全局色彩和谐 + 色盲校验 → 输出可编辑矢量（SVG）

------

## 2）客户文字需求 → AI 绘图流程（Text-to-Workflow）

**工作内容**：生 / 化 / 环 / 材客户模糊文本 → 结构化、可执行的**AI 绘图流程**（含元素、关系、视角、风格、步骤）。

### 核心痛点

- 自然语言模糊、多义、缺细节；专业术语不统一；逻辑关系隐含（因果 / 空间 / 时序）。

### 相关算法 / 技术

- 大语言模型 LLM（语义理解 + 结构化）
  - **Gemini 3 Pro / GPT-4o / DeepSeek**：做**需求解析→实体抽取→关系抽取→流程生成**
  - Prompt 工程：把 “文字描述” 翻译成**图注式结构化 prompt**（对象 + 数量 + 空间关系 + 视角 + 风格 + 输出格式）
- 知识图谱（KG）
  - 生 / 化 / 环 / 材领域本体：细胞、分子、反应、器件、环境单元及其标准关系库
  - 把客户文本映射到 KG → 补全隐含实体与关系 → 生成规范流程
- 文本→流程模板映射
  - 预定义模板库：机制图、流程图、架构图、通路图、反应式、器件结构
  - LLM 选择模板 + 填充参数 → 输出 “步骤 1/2/3 + 元素清单 + 空间约束”

### 常用模型

- Gemini 3 Pro Image（Nano Banana Pro）：**理解科研图逻辑（箭头 / 层级 / 因果）**最强
- GPT-4o + Vision：图文对照、迭代优化流程

### 输出示例（AI 绘图流程）

1. 元素：细胞膜、受体、配体、信号分子、细胞核
2. 关系：配体→受体（结合）→信号分子（激活）→细胞核（转录）
3. 视角：2D 平面、俯视、线条简约、无透视
4. 风格：Nature 级示意图、矢量、白底、低饱和度
5. 步骤：元素绘制 → 布局 → 箭头 → 标注 → 配色 → 导出 SVG

------

## 3）AI 流程 → 元素名称 + 绘制元素图形（Element Extraction & Generation）

**工作内容**：从流程中提取**原子元素**（如 “线粒体、苯环、纳米颗粒、反应器”），生成标准科研矢量图元。

### 相关算法 / 技术

- 命名实体识别 NER（科研领域）
  - LLM / 领域微调 BERT：抽取**生物实体、化学结构、材料单元、环境设备**
  - 输出：元素名称列表（标准化术语，如 “磷脂双分子层” 而非 “膜”）
- 文本→矢量元素生成（Text-to-SVG）
  - **专用生成模型**：AcaDraw、plykit.ai、BioRender 后端模型
  - **Diffusion + 矢量化**：SDXL 生成栅格 → SAM 分割 → 轮廓提取 → SVG 优化
  - **分子 / 晶体专用**：SMILES→结构（RDKit/OpenBabel）、球棍 / 填充模型自动生成
- 元素库检索与复用
  - 矢量图库（SVG）：细胞、细胞器、分子、晶体、器件、流程图标
  - 相似度检索：CLIP 特征匹配 → 召回最相似元素 → 微调复用

### 常用工具

- AcaDraw、BioRender、ChemDraw、PyMOL、RDKit
- AutoFIGURE-EDIT：生成**可编辑 SVG 科研图**（分割→资产提取→模板生成→注入）

------

## 4）元素 + 关系 → 空间布局 / 大小 / 视角 → 科研配图（Layout & Composition）

**工作内容**：按流程定义的**空间关系、层级、比例、视角**，自动构图，生成完整科研示意图。

### 相关算法 / 技术

- 空间关系推理（Spatial Reasoning）
  - LLM + 空间知识库：解析 “包裹、嵌入、相邻、上下游、中心 - 外围、层叠”
  - 输出：图结构（节点 = 元素，边 = 空间 / 逻辑关系）
- 自动布局算法（Graph Layout）
  - **力导向布局（Force-directed）**：节点斥力、关系引力 → 均匀分布（通路 / 网络）
  - **层级布局（Hierarchical）**：自上而下 / 自左向右（流程、反应步骤）
  - **网格布局（Grid）**：规则阵列（材料结构、器件阵列）
  - **径向布局（Radial）**：中心辐射（细胞信号、中心器官）
- 尺寸 / 比例约束
  - 领域规则：细胞 > 细胞器 > 分子；反应器 > 管道 > 阀门
  - 视觉层级：关键元素放大、次要缩小；强调色突出重点
- 视角与投影
  - 2D：正视图、俯视图、剖面图（最常用）
  - 2.5D：等轴测（Isometric），材料 / 器件常用
  - 3D：透视 / 正交（分子、晶体、细胞器）
- 美学优化
  - 留白：≥15% 空白区域
  - 对齐 / 分布：网格对齐、等间距
  - 视觉平衡：左右 / 上下重量均衡

### 常用模型 / 工具

- AutoFIGURE-EDIT：结构索引→SVG 模板生成→优化布局
- Graphviz、Mermaid、TikZ：代码式布局
- Adobe Illustrator：AI 辅助布局 + 手动微调

### 典型构图流程

1. 建图：元素为节点，关系为边
2. 选布局：流程→层级；通路→力导向；结构→网格 / 径向
3. 定尺寸：按领域比例缩放
4. 设视角：2D / 等轴测 / 3D
5. 优化：对齐、留白、平衡 → 输出完整配图

------

## 5）成品图 → 元素名称标注 + 图形定位（Visual Grounding）

**工作内容**：对已完成的科研图，**标注所有元素名称**并**在图中定位对应图形**（用于图例、索引、质控）。

### 相关算法 / 技术

- 视觉定位（Visual Grounding）

  ：文本→图中区域

  - **GroundingDINO**：文本提示→目标检测框（开放词汇、科研实体友好）
  - **SAM（Segment Anything）**：框 / 点提示→**像素级掩码**，精准分割元素轮廓
  - 组合：GroundingDINO（粗定位）+ SAM（精分割）→ 元素掩码 + 边界框

- 图文匹配（Image-Text Retrieval）

  - CLIP/ALIGN：元素名称文本 ↔ 图中区域特征 → 相似度匹配 → 标注

- OCR + 知识对齐

  - 提取图中已有标注 → LLM 标准化术语 → 与元素库对齐

### 常用模型

- GroundingDINO + SAM 3：科研图元素分割标杆
- CLIP：跨模态检索匹配

### 实现步骤

1. 输入：成品图 + 元素名称列表
2. GroundingDINO：每个名称 → 检测框
3. SAM：框 → 精确掩码（轮廓）
4. 输出：元素名称 + 坐标框 + 掩码 → 标注图层 / 图例

------

## 总结：整体 AI 科研绘图技术栈

1. 线稿着色：**SketchDeco/SDXL+ControlNet + 色盲配色**
2. 需求→流程：**Gemini 3 Pro/LLM + 领域知识图谱 + 模板库**
3. 元素生成：**NER + Text-to-SVG（AcaDraw/BioRender）+ 元素库**
4. 构图布局：**空间推理 + 力导向 / 层级 / 网格布局 + 视角投影**
5. 标注定位：**GroundingDINO + SAM + CLIP 图文匹配**